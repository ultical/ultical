package de.ultical.backend.schedule;

import java.util.Map;

import de.ultical.backend.model.Team;

public class ConcreteSchedule {

    private Map<Integer, Team> initialSeeding;

    // public ConcreteSchedule(Schedule format) {
    // this.setNumberTeams(format.getNumberTeams());
    //
    // List<Phase> concretePhases = new ArrayList<Phase>();
    // for (Phase PhaseFormat : format.getPhases()) {
    // concretePhases.add(new ConcretePhase(PhaseFormat));
    // }
    // this.setPhases(concretePhases);
    //
    // this.setSeedingPhase(new ConcretePhase(format.getSeedingPhase()));
    // }

}
